name = "sieve-sync"
main = "worker.js"
compatibility_date = "2026-01-01"

# 1. Create the KV namespace once:
#      wrangler kv namespace create SIEVE_SYNC
#    It prints an `id = "..."` — paste it below.
[[kv_namespaces]]
binding = "SIEVE_SYNC"
id = "PASTE_YOUR_KV_NAMESPACE_ID_HERE"
